/**
 * @file Model Context Protocol (MCP) Domain Module Entry Point.
 *
 * Re-exports strict Zod schemas, error codes, role/scope enums,
 * and envelopes for the MCP layer.
 */

export * from './mcp.schemas.js';
